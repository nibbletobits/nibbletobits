## function that takes a word from the main.py and prints each charecter vertically
# @parm (word) is a veriableset in main as a string.
# the function does not return a value

def vert_word(word=""):
    for c in word:
        print(c)
    return
